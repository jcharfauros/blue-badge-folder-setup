/*
SPREAD OPERATOR
    - The spread operator pulls out all elements in an array and gives them back to you as a standalone list of elements.

    - It takes and iterable object and/or object and expands it into individual elements. The spread operator is typically used to make copies of an array/object.
    - Denoted by the ellipsis(...)
*/

let studnets = ["Jake Greene", "Stephanie Falls"];
//WRONG SYNTAX
//...students

// let copiedStudents = [...students];
console.log(copiedStudents);
console.log(students, copiedStudents);